Implemented Iteration 3 dynamic multi-house support without committing.

Changed:

- `src/shared/types.ts`
- `src/server/game.ts`
- `src/server/server.ts`
- `src/client/client.ts`
- `src/client/index.html`
- `src/client/styles.css`
- `src/smoke/smoke.ts`
- `README.md`
- `CODEX_HANDOFF.md`

Key decisions:

- One character per player per house.
- Listing fees are based on listing duration × house fee rate; completed-sale proceeds also use the house rate.
- Featured wins award fame equal to 25% of the winning bid, rounded up.
- Specialty houses deterministically generate at least three specialty-tagged orders out of six.
- A capped listing spawns a house only when no existing house has spare capacity.
- Schema-v1 snapshots migrate into Grand Exchange, preserving legacy auctions, orders, players, and character membership.
- Existing untracked `.hermes/` and `docs/` content was left untouched.

Exact smoke result:

```text
SMOKE PASS: house 1 booted; registration and deployment succeeded; JOIN-before-deploy rejected; HOUSE_CAP=1 listing spawned house 2; second character deployed cross-house; redeploy set a 24h cooldown and blocked abilities; featured promotion settled with +25 fame; specialty orders met 50% bias; season reset kept houses/deployments, cleared cooldowns, and regenerated 6 orders per house; schema-v2 persistence preserved houses/deployments; public house detail preserved seller/fake privacy.
```

`npm.cmd run smoke`, its nested strict TypeScript build, and `git diff --check` all pass.

Deviation: browser/device visual QA was not run; the responsive house browser, redeployment controls, switcher, cooldown state, and featured styling should receive a manual desktop/mobile pass.

Handoff status:

- `CODEX_HANDOFF.md` updated.
- `DEBUG_HANDOFF.md` not created because no repeated unresolved defect remains.
- No runtime dependencies added.
- No git commit created.